{
  "name": "claude-mcp-server",
  "version": "0.5.0",
  "description": "MCP server — Claude CLI command builder for Cursor agents",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tal-hason/claude-mcp-server.git"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "setup": "bash setup.sh"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
